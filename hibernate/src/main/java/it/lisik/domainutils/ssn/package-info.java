@TypeDefs(
        @TypeDef(typeClass = SSNType.class, defaultForType = SSN.class, name = "SSN")
)
package it.lisik.domainutils.ssn;

import org.hibernate.annotations.TypeDef;
import org.hibernate.annotations.TypeDefs;