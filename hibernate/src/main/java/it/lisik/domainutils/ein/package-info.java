@TypeDefs(
        @TypeDef(typeClass = EINType.class, defaultForType = EIN.class, name = "EIN")
)
package it.lisik.domainutils.ein;

import org.hibernate.annotations.TypeDef;
import org.hibernate.annotations.TypeDefs;